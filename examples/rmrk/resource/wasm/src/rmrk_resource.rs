// Code generated by sails-client-gen-v2. DO NOT EDIT.
#[allow(unused_imports)]
use sails::{client::*, collections::*, prelude::*};
pub struct RmrkResourceProgram;

impl RmrkResourceProgram {
    pub const ROUTE_ID_RMRK_RESOURCE: u8 = 1;
}

impl sails::client::Program for RmrkResourceProgram {}

pub trait RmrkResource {
    type Env: sails::client::GearEnv;
    fn rmrk_resource(
        &self,
    ) -> sails::client::Service<rmrk_resource::RmrkResourceImpl, Self::Env>;
}

impl<E: sails::client::GearEnv> RmrkResource
    for sails::client::Actor<RmrkResourceProgram, E>
{
    type Env = E;
    fn rmrk_resource(
        &self,
    ) -> sails::client::Service<rmrk_resource::RmrkResourceImpl, Self::Env> {
        self.service(RmrkResourceProgram::ROUTE_ID_RMRK_RESOURCE)
    }
}
pub trait RmrkResourceCtors {
    type Env: sails::client::GearEnv + sails::client::EnvWithCtor;
    #[allow(clippy::new_ret_no_self)]
    #[allow(clippy::wrong_self_convention)]
    fn new(self) -> sails::client::PendingCtor<RmrkResourceProgram, io::New, Self::Env>;
}

impl<E: sails::client::GearEnv + sails::client::EnvWithCtor> RmrkResourceCtors
    for sails::client::Deployment<RmrkResourceProgram, E>
{
    type Env = E;
    fn new(self) -> sails::client::PendingCtor<RmrkResourceProgram, io::New, Self::Env> {
        self.pending_ctor(())
    }
}

pub mod io {
    use super::*;
    sails::io_struct_impl!(New () -> (), 0);
}

pub mod rmrk_resource {
    use super::*;

    #[sails::sails_type(crate = sails)]
    #[derive(PartialEq, Clone, Debug)]
    pub enum Error {
        NotAuthorized,
        ZeroResourceId,
        ResourceAlreadyExists,
        ResourceNotFound,
        WrongResourceType,
        PartNotFound,
    }
    #[sails::sails_type(crate = sails)]
    #[derive(PartialEq, Clone, Debug)]
    pub enum Resource {
        Basic(BasicResource),
        Slot(SlotResource),
        Composed(ComposedResource),
    }
    #[sails::sails_type(crate = sails)]
    #[derive(PartialEq, Clone, Debug)]
    pub struct BasicResource {
        /// URI like IPFS hash
        pub src: String,
        /// If the resource has the thumb property, this will be a URI to a thumbnail of the given
        /// resource.
        pub thumb: Option<String>,
        /// Reference to IPFS location of metadata
        pub metadata_uri: String,
    }
    #[sails::sails_type(crate = sails)]
    #[derive(PartialEq, Clone, Debug)]
    pub struct SlotResource {
        /// URI like ipfs hash
        pub src: String,
        /// If the resource has the thumb property, this will be a URI to a thumbnail of the given
        /// resource.
        pub thumb: String,
        /// Reference to IPFS location of metadata
        pub metadata_uri: String,
        pub base: ActorId,
        /// If the resource has the slot property, it was designed to fit into a specific Base's slot.
        pub slot: u32,
    }
    #[sails::sails_type(crate = sails)]
    #[derive(PartialEq, Clone, Debug)]
    pub struct ComposedResource {
        /// URI like ipfs hash
        pub src: String,
        /// If the resource has the thumb property, this will be a URI to a thumbnail of the given
        /// resource.
        pub thumb: String,
        /// Reference to IPFS location of metadata
        pub metadata_uri: String,
        pub base: ActorId,
        pub parts: Vec<u32>,
    }

    pub trait RmrkResource {
        type Env: sails::client::GearEnv;
        fn add_part_to_resource(
            &mut self,
            resource_id: u8,
            part_id: u32,
        ) -> sails::client::PendingCall<io::AddPartToResource, Self::Env>;
        fn add_resource_entry(
            &mut self,
            resource_id: u8,
            resource: Resource,
        ) -> sails::client::PendingCall<io::AddResourceEntry, Self::Env>;
        fn resource(
            &self,
            resource_id: u8,
        ) -> sails::client::PendingCall<io::Resource, Self::Env>;
    }

    pub struct RmrkResourceImpl;

    impl sails::client::Identifiable for RmrkResourceImpl {
        const INTERFACE_ID: sails::InterfaceId =
            sails::InterfaceId::from_bytes_8([215, 56, 96, 51, 70, 205, 63, 27]);
    }

    impl<E: sails::client::GearEnv> RmrkResource for sails::client::Service<RmrkResourceImpl, E> {
        type Env = E;
        fn add_part_to_resource(
            &mut self,
            resource_id: u8,
            part_id: u32,
        ) -> sails::client::PendingCall<io::AddPartToResource, Self::Env> {
            self.pending_call((resource_id, part_id))
        }
        fn add_resource_entry(
            &mut self,
            resource_id: u8,
            resource: Resource,
        ) -> sails::client::PendingCall<io::AddResourceEntry, Self::Env> {
            self.pending_call((resource_id, resource))
        }
        fn resource(
            &self,
            resource_id: u8,
        ) -> sails::client::PendingCall<io::Resource, Self::Env> {
            self.pending_call((resource_id,))
        }
    }

    pub mod io {
        use super::*;
        sails::io_struct_impl!(AddPartToResource (resource_id: u8, part_id: u32) -> super::Result<u32, super::Error, >, 0, <super::RmrkResourceImpl as sails::client::Identifiable>::INTERFACE_ID);
        sails::io_struct_impl!(AddResourceEntry (resource_id: u8, resource: super::Resource) -> super::Result<(u8, super::Resource, ), super::Error, >, 1, <super::RmrkResourceImpl as sails::client::Identifiable>::INTERFACE_ID);
        sails::io_struct_impl!(Resource (resource_id: u8) -> super::Result<super::Resource, super::Error, >, 2, <super::RmrkResourceImpl as sails::client::Identifiable>::INTERFACE_ID);
    }

    #[cfg(not(target_arch = "wasm32"))]
    pub mod events {
        use super::*;
        #[sails::sails_type(crate = sails)]
        #[derive(PartialEq, Debug)]
        pub enum RmrkResourceEvents {
            #[codec(index = 0)]
            PartAdded { resource_id: u8, part_id: u32 },
            #[codec(index = 1)]
            ResourceAdded { resource_id: u8 },
        }

        impl RmrkResourceEvents {
            pub fn entry_id(&self) -> u16 {
                match self {
                    Self::PartAdded { .. } => 0,
                    Self::ResourceAdded { .. } => 1,
                }
            }
        }

        impl sails::client::Event for RmrkResourceEvents {
            fn decode_event(
                route: &sails::client::RouteIdx,
                payload: impl AsRef<[u8]>,
            ) -> Result<Self, sails::scale_codec::Error> {
                sails::client::decode_event_v2::<Self>(route.0, payload)
            }
        }

        impl sails::client::Identifiable for RmrkResourceEvents {
            const INTERFACE_ID: sails::InterfaceId =
                <RmrkResourceImpl as sails::client::Identifiable>::INTERFACE_ID;
        }

        impl sails::client::ServiceWithEvents for RmrkResourceImpl {
            type Event = RmrkResourceEvents;
        }
    }
}
