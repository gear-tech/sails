constructor {
  WithInitialValue : (init_value: u64);
};

service Dec {
  Op : (val: u64) -> u64;
};

service Inc {
  Op : (val: u64) -> u64;
};

service Query {
  query CurrentValue : () -> u64;
};

