[package]
name = "event-routes-app"
version = "0.1.0"
edition.workspace = true

[dependencies]
sails-rs.workspace = true

[build-dependencies]
sails-rs = { workspace = true, features = ["wasm-builder"] }
