[package]
name = "partial-idl-client"
version = "0.1.0"
edition.workspace = true

[dependencies]
sails-rs.workspace = true

[build-dependencies]
sails-rs = { workspace = true, features = ["client-builder"] }
partial-idl-app = { path = "../app" }
