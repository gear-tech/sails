[package]
name = "{{ app-project-name }}"
version = "0.1.0"
edition = "2021"

[dependencies]
sails-rs = "{{ sails-rs-version }}"
# This needs be removed as soon as fixed version of Gear crates are released
gear-wasm-instrument = "=1.5.0"
