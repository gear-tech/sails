{
  "name": "program",
  "private": true,
  "type": "module",
  "devDependencies": {
    "@gear-js/api": "0.44.1",
    "@polkadot/api": "16.4.6",
    "sails-js": "0.5.0",
    "sails-js-cli": "0.5.0",
    "typescript": "^5.9.2"
  },
  "scripts": {
    "build": "node ../cli/build/app.js generate ../../examples/demo/client/demo_client.idl -o src --no-project -y && tsc",
    "clean": "rm -rf lib"
  }
}
