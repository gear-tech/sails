{
  "name": "program",
  "private": true,
  "type": "module",
  "devDependencies": {
    "@gear-js/api": "^0.42.0",
    "@polkadot/api": "^16.2.1",
    "sails-js": "*",
    "sails-js-cli": "*",
    "typescript": "^5.8.3"
  },
  "scripts": {
    "build": "node ../cli/build/app.js generate ../../examples/demo/client/demo.idl -o src --no-project -y && tsc",
    "clean": "rm -rf lib"
  }
}
