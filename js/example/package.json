{
  "name": "program",
  "private": true,
  "type": "module",
  "devDependencies": {
    "@gear-js/api": "^0.43.0",
    "@polkadot/api": "^16.4.3",
    "sails-js": "*",
    "sails-js-cli": "*",
    "typescript": "^5.9.2"
  },
  "scripts": {
    "build": "node ../cli/build/app.js generate ../../examples/demo/client/demo_client.idl -o src --no-project -y && tsc",
    "clean": "rm -rf lib"
  }
}
