{
  "name": "sails-js-example",
  "private": true,
  "type": "module",
  "devDependencies": {
    "@gear-js/api": "0.45.0",
    "@polkadot/api": "16.5.6",
    "sails-js": "workspace:*",
    "sails-js-cli": "workspace:*"
  },
  "scripts": {
    "build": "echo node ../cli/build/app.js generate ../../examples/demo/client/demo_client.idl -o src --no-project -y && tsc",
    "clean": "rm -rf lib"
  }
}
