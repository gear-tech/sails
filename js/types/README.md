# Overview
The `sails-js-types` library provides shared TypeScript types that are bundled into [`sails-js`](../README.md) and available via the `sails-js/types` subpath. This package is no longer published separately.
