# Overview
The `sails-js-util` library contains utility functions that are bundled into [`sails-js`](../README.md) and available via the `sails-js/util` subpath. This package is no longer published separately.
