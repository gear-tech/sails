program AllocStress {
    constructors {
        NewForBench();
    }
    services {
        AllocStress,
    }
    types {
        struct AllocStressResult {
            inner: Vec<u8>,
        }
    }
}

service AllocStress {
    functions {
        AllocStress(n: u32) -> AllocStressResult;
    }
}