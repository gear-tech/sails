type AllocStressResult = struct {
  inner: vec u8,
};

constructor {
  NewForBench : ();
};

service AllocStress {
  /// !@interface_id = 0x81d46853166d16db
  /// !@entry_id = 0x0001
  AllocStress : (n: u32) -> AllocStressResult;
};

