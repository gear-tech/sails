type ComputeStressResult = struct {
  res: u32,
};

constructor {
  NewForBench : ();
};

service ComputeStress {
  /// !@interface_id = 0xf8aa07c8c4b0189f
  /// !@entry_id = 0x0001
  ComputeStress : (n: u32) -> ComputeStressResult;
};

