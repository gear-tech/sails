program ComputeStress {
    constructors {
        NewForBench();
    }
    services {
        ComputeStress,
    }
}

service ComputeStress {
    functions {
        ComputeStress(n: u32) -> ComputeStressResult;
    }
    types {
        struct ComputeStressResult {
            res: u32,
        }
    }
}
