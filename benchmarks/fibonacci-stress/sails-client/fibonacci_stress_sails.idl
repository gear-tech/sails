/// Wrapper over a [`stress_fibo`] buffer result.
type FiboStressResult = struct {
  inner: vec u8,
};

constructor {
  New : ();
};

service FiboStress {
  StressBytes : (n: u32) -> vec u8;
  StressFibo : (n: u32) -> FiboStressResult;
};

