// Code generated by sails-client-gen-v2. DO NOT EDIT.
#[allow(unused_imports)]
use sails_rs::{client::*, collections::*, prelude::*};
pub struct MinimalVftSailsProgram;

impl MinimalVftSailsProgram {
    pub const ROUTE_ID_VFT: u8 = 1;
}

impl sails_rs::client::Program for MinimalVftSailsProgram {}

pub trait MinimalVftSails {
    type Env: sails_rs::client::GearEnv;
    fn vft(&self) -> sails_rs::client::Service<vft::VftImpl, Self::Env>;
}

impl<E: sails_rs::client::GearEnv> MinimalVftSails
    for sails_rs::client::Actor<MinimalVftSailsProgram, E>
{
    type Env = E;
    fn vft(&self) -> sails_rs::client::Service<vft::VftImpl, Self::Env> {
        self.service(MinimalVftSailsProgram::ROUTE_ID_VFT)
    }
}
pub trait MinimalVftSailsCtors {
    type Env: sails_rs::client::GearEnv;
    fn new_for_bench(
        self,
    ) -> sails_rs::client::PendingCtor<MinimalVftSailsProgram, io::NewForBench, Self::Env>;
}

impl<E: sails_rs::client::GearEnv> MinimalVftSailsCtors
    for sails_rs::client::Deployment<MinimalVftSailsProgram, E>
{
    type Env = E;
    fn new_for_bench(
        self,
    ) -> sails_rs::client::PendingCtor<MinimalVftSailsProgram, io::NewForBench, Self::Env> {
        self.pending_ctor(())
    }
}

pub mod io {
    use super::*;
    sails_rs::io_struct_impl!(NewForBench () -> (), 0);
}

pub mod vft {
    use super::*;

    pub trait Vft {
        type Env: sails_rs::client::GearEnv;
        fn allowance(
            &mut self,
            owner: ActorId,
            spender: ActorId,
        ) -> sails_rs::client::PendingCall<io::Allowance, Self::Env>;
        fn approve(
            &mut self,
            spender: ActorId,
            amount: U256,
        ) -> sails_rs::client::PendingCall<io::Approve, Self::Env>;
        fn balance_of(
            &mut self,
            owner: ActorId,
        ) -> sails_rs::client::PendingCall<io::BalanceOf, Self::Env>;
        fn transfer(
            &mut self,
            to: ActorId,
            amount: U256,
        ) -> sails_rs::client::PendingCall<io::Transfer, Self::Env>;
        fn transfer_from(
            &mut self,
            owner: ActorId,
            to: ActorId,
            amount: U256,
        ) -> sails_rs::client::PendingCall<io::TransferFrom, Self::Env>;
    }

    pub struct VftImpl;

    impl sails_rs::client::Identifiable for VftImpl {
        const INTERFACE_ID: sails_rs::InterfaceId =
            sails_rs::InterfaceId::from_bytes_8([83, 187, 140, 26, 152, 56, 126, 212]);
    }

    impl<E: sails_rs::client::GearEnv> Vft for sails_rs::client::Service<VftImpl, E> {
        type Env = E;
        fn allowance(
            &mut self,
            owner: ActorId,
            spender: ActorId,
        ) -> sails_rs::client::PendingCall<io::Allowance, Self::Env> {
            self.pending_call((owner, spender))
        }
        fn approve(
            &mut self,
            spender: ActorId,
            amount: U256,
        ) -> sails_rs::client::PendingCall<io::Approve, Self::Env> {
            self.pending_call((spender, amount))
        }
        fn balance_of(
            &mut self,
            owner: ActorId,
        ) -> sails_rs::client::PendingCall<io::BalanceOf, Self::Env> {
            self.pending_call((owner,))
        }
        fn transfer(
            &mut self,
            to: ActorId,
            amount: U256,
        ) -> sails_rs::client::PendingCall<io::Transfer, Self::Env> {
            self.pending_call((to, amount))
        }
        fn transfer_from(
            &mut self,
            owner: ActorId,
            to: ActorId,
            amount: U256,
        ) -> sails_rs::client::PendingCall<io::TransferFrom, Self::Env> {
            self.pending_call((owner, to, amount))
        }
    }

    pub mod io {
        use super::*;
        sails_rs::io_struct_impl!(Allowance (owner: ActorId, spender: ActorId) -> U256, 0, <super::VftImpl as sails_rs::client::Identifiable>::INTERFACE_ID);
        sails_rs::io_struct_impl!(Approve (spender: ActorId, amount: U256) -> bool, 1, <super::VftImpl as sails_rs::client::Identifiable>::INTERFACE_ID);
        sails_rs::io_struct_impl!(BalanceOf (owner: ActorId) -> U256, 2, <super::VftImpl as sails_rs::client::Identifiable>::INTERFACE_ID);
        sails_rs::io_struct_impl!(Transfer (to: ActorId, amount: U256) -> bool, 3, <super::VftImpl as sails_rs::client::Identifiable>::INTERFACE_ID);
        sails_rs::io_struct_impl!(TransferFrom (owner: ActorId, to: ActorId, amount: U256) -> bool, 4, <super::VftImpl as sails_rs::client::Identifiable>::INTERFACE_ID);
    }
}
