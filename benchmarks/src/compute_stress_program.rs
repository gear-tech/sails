// Code generated by sails-client-gen-v2. DO NOT EDIT.
#[allow(unused_imports)]
use sails::{client::*, collections::*, prelude::*};
pub struct ComputeStressProgram;

impl ComputeStressProgram {
    pub const ROUTE_ID_COMPUTE_STRESS: u8 = 1;
}

impl sails::client::Program for ComputeStressProgram {}

pub trait ComputeStress {
    type Env: sails::client::GearEnv;
    fn compute_stress(
        &self,
    ) -> sails::client::Service<compute_stress::ComputeStressImpl, Self::Env>;
}

impl<E: sails::client::GearEnv> ComputeStress
    for sails::client::Actor<ComputeStressProgram, E>
{
    type Env = E;
    fn compute_stress(
        &self,
    ) -> sails::client::Service<compute_stress::ComputeStressImpl, Self::Env> {
        self.service(ComputeStressProgram::ROUTE_ID_COMPUTE_STRESS)
    }
}
pub trait ComputeStressCtors {
    type Env: sails::client::GearEnv + sails::client::EnvWithCtor;
    fn new_for_bench(
        self,
    ) -> sails::client::PendingCtor<ComputeStressProgram, io::NewForBench, Self::Env>;
}

impl<E: sails::client::GearEnv + sails::client::EnvWithCtor> ComputeStressCtors
    for sails::client::Deployment<ComputeStressProgram, E>
{
    type Env = E;
    fn new_for_bench(
        self,
    ) -> sails::client::PendingCtor<ComputeStressProgram, io::NewForBench, Self::Env> {
        self.pending_ctor(())
    }
}

pub mod io {
    use super::*;
    sails::io_struct_impl!(NewForBench () -> (), 0);
}

pub mod compute_stress {
    use super::*;

    #[sails::sails_type(crate = sails)]
    #[derive(PartialEq, Clone, Debug)]
    pub struct ComputeStressResult {
        pub res: u32,
    }

    pub trait ComputeStress {
        type Env: sails::client::GearEnv;
        fn compute_stress(
            &mut self,
            n: u32,
        ) -> sails::client::PendingCall<io::ComputeStress, Self::Env>;
    }

    pub struct ComputeStressImpl;

    impl sails::client::Identifiable for ComputeStressImpl {
        const INTERFACE_ID: sails::InterfaceId =
            sails::InterfaceId::from_bytes_8([254, 138, 70, 56, 122, 195, 121, 54]);
    }

    impl<E: sails::client::GearEnv> ComputeStress
        for sails::client::Service<ComputeStressImpl, E>
    {
        type Env = E;
        fn compute_stress(
            &mut self,
            n: u32,
        ) -> sails::client::PendingCall<io::ComputeStress, Self::Env> {
            self.pending_call((n,))
        }
    }

    pub mod io {
        use super::*;
        sails::io_struct_impl!(ComputeStress (n: u32) -> super::ComputeStressResult, 0, <super::ComputeStressImpl as sails::client::Identifiable>::INTERFACE_ID);
    }
}
