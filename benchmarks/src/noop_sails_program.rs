// Code generated by sails-client-gen-v2. DO NOT EDIT.
#[allow(unused_imports)]
use sails_rs::{client::*, collections::*, prelude::*};
pub struct NoopSailsProgram;

impl NoopSailsProgram {
    pub const ROUTE_ID_NOOP_SAILS: u8 = 1;
}

impl sails_rs::client::Program for NoopSailsProgram {}

pub trait NoopSails {
    type Env: sails_rs::client::GearEnv;
    fn noop_sails(&self) -> sails_rs::client::Service<noop_sails::NoopSailsImpl, Self::Env>;
}

impl<E: sails_rs::client::GearEnv> NoopSails for sails_rs::client::Actor<NoopSailsProgram, E> {
    type Env = E;
    fn noop_sails(&self) -> sails_rs::client::Service<noop_sails::NoopSailsImpl, Self::Env> {
        self.service(NoopSailsProgram::ROUTE_ID_NOOP_SAILS)
    }
}
pub trait NoopSailsCtors {
    type Env: sails_rs::client::GearEnv;
    fn new_for_bench(
        self,
    ) -> sails_rs::client::PendingCtor<NoopSailsProgram, io::NewForBench, Self::Env>;
}

impl<E: sails_rs::client::GearEnv> NoopSailsCtors
    for sails_rs::client::Deployment<NoopSailsProgram, E>
{
    type Env = E;
    fn new_for_bench(
        self,
    ) -> sails_rs::client::PendingCtor<NoopSailsProgram, io::NewForBench, Self::Env> {
        self.pending_ctor(())
    }
}

pub mod io {
    use super::*;
    sails_rs::io_struct_impl!(NewForBench () -> (), 0);
}

pub mod noop_sails {
    use super::*;

    pub trait NoopSails {
        type Env: sails_rs::client::GearEnv;
        fn noop(&mut self) -> sails_rs::client::PendingCall<io::Noop, Self::Env>;
    }

    pub struct NoopSailsImpl;

    impl sails_rs::client::Identifiable for NoopSailsImpl {
        const INTERFACE_ID: sails_rs::InterfaceId =
            sails_rs::InterfaceId::from_bytes_8([84, 87, 204, 48, 221, 148, 206, 41]);
    }

    impl<E: sails_rs::client::GearEnv> NoopSails for sails_rs::client::Service<NoopSailsImpl, E> {
        type Env = E;
        fn noop(&mut self) -> sails_rs::client::PendingCall<io::Noop, Self::Env> {
            self.pending_call(())
        }
    }

    pub mod io {
        use super::*;
        sails_rs::io_struct_impl!(Noop () -> bool, 0, <super::NoopSailsImpl as sails_rs::client::Identifiable>::INTERFACE_ID);
    }
}
