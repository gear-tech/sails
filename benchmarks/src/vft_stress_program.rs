// Code generated by sails-client-gen-v2. DO NOT EDIT.
#[allow(unused_imports)]
use sails_rs::{client::*, collections::*, prelude::*};
pub struct VftStressProgram;

impl VftStressProgram {
    pub const ROUTE_ID_VFT_STRESS: u8 = 1;
}

impl sails_rs::client::Program for VftStressProgram {}

pub trait VftStress {
    type Env: sails_rs::client::GearEnv;
    fn vft_stress(&self) -> sails_rs::client::Service<vft_stress::VftStressImpl, Self::Env>;
}

impl<E: sails_rs::client::GearEnv> VftStress for sails_rs::client::Actor<VftStressProgram, E> {
    type Env = E;
    fn vft_stress(&self) -> sails_rs::client::Service<vft_stress::VftStressImpl, Self::Env> {
        self.service(VftStressProgram::ROUTE_ID_VFT_STRESS)
    }
}
pub trait VftStressCtors {
    type Env: sails_rs::client::GearEnv;
    fn new_for_bench(
        self,
    ) -> sails_rs::client::PendingCtor<VftStressProgram, io::NewForBench, Self::Env>;
}

impl<E: sails_rs::client::GearEnv> VftStressCtors
    for sails_rs::client::Deployment<VftStressProgram, E>
{
    type Env = E;
    fn new_for_bench(
        self,
    ) -> sails_rs::client::PendingCtor<VftStressProgram, io::NewForBench, Self::Env> {
        self.pending_ctor(())
    }
}

pub mod io {
    use super::*;
    sails_rs::io_struct_impl!(NewForBench () -> (), 0);
}

pub mod vft_stress {
    use super::*;

    #[sails_rs::sails_type(crate = sails_rs)]
    #[derive(PartialEq, Clone, Debug)]
    pub enum VftPhase {
        ProbeOverhead,
        NoopBody,
        EchoBody,
        KeyDerive,
        AllowanceGet,
        AllowancePut,
        BalanceGetFrom,
        BalanceGetTo,
        BalancePutFrom,
        BalancePutTo,
        BalanceTransfer,
        BalanceTransferFrom,
        ResultBuild,
    }
    #[sails_rs::sails_type(crate = sails_rs)]
    #[derive(PartialEq, Clone, Debug)]
    pub struct VftPhaseGas {
        pub phase: VftPhase,
        pub gas: u64,
    }
    #[sails_rs::sails_type(crate = sails_rs)]
    #[derive(PartialEq, Clone, Debug)]
    pub struct VftProfileResult {
        pub result: VftTransferResult,
        pub phases: Vec<VftPhaseGas>,
    }
    #[sails_rs::sails_type(crate = sails_rs)]
    #[derive(PartialEq, Clone, Debug)]
    pub enum VftStorageBackend {
        BTree,
        HashMap,
        SailsFixed,
        SailsStatic,
        SailsStaticFast,
    }
    #[sails_rs::sails_type(crate = sails_rs)]
    #[derive(PartialEq, Clone, Debug)]
    pub enum VftTransferOp {
        Transfer,
        TransferFrom,
    }
    #[sails_rs::sails_type(crate = sails_rs)]
    #[derive(PartialEq, Clone, Debug)]
    pub struct VftTransferResult {
        pub from_balance: U256,
        pub to_balance: U256,
        pub allowance: U256,
        pub balance_len: u32,
        pub allowance_len: u32,
        pub transferred: bool,
    }

    pub trait VftStress {
        type Env: sails_rs::client::GearEnv;
        fn bench_echo_vft_args(
            &mut self,
            backend: VftStorageBackend,
            op: VftTransferOp,
            seed: u32,
        ) -> sails_rs::client::PendingCall<io::BenchEchoVftArgs, Self::Env>;
        fn bench_noop(&mut self) -> sails_rs::client::PendingCall<io::BenchNoop, Self::Env>;
        fn bench_vft_approve_bool(
            &mut self,
            backend: VftStorageBackend,
            owner_seed: u32,
            spender_seed: u32,
        ) -> sails_rs::client::PendingCall<io::BenchVftApproveBool, Self::Env>;
        fn bench_vft_approve_profile(
            &mut self,
            backend: VftStorageBackend,
            owner_seed: u32,
            spender_seed: u32,
        ) -> sails_rs::client::PendingCall<io::BenchVftApproveProfile, Self::Env>;
        fn bench_vft_transfer(
            &mut self,
            backend: VftStorageBackend,
            op: VftTransferOp,
            seed: u32,
        ) -> sails_rs::client::PendingCall<io::BenchVftTransfer, Self::Env>;
        fn bench_vft_transfer_fresh_bool(
            &mut self,
            backend: VftStorageBackend,
            seed: u32,
        ) -> sails_rs::client::PendingCall<io::BenchVftTransferFreshBool, Self::Env>;
        fn bench_vft_transfer_fresh_profile(
            &mut self,
            backend: VftStorageBackend,
            seed: u32,
        ) -> sails_rs::client::PendingCall<io::BenchVftTransferFreshProfile, Self::Env>;
        fn bench_vft_transfer_profile(
            &mut self,
            backend: VftStorageBackend,
            op: VftTransferOp,
            seed: u32,
        ) -> sails_rs::client::PendingCall<io::BenchVftTransferProfile, Self::Env>;
        fn prepare_vft(
            &mut self,
            backend: VftStorageBackend,
            len: u32,
        ) -> sails_rs::client::PendingCall<io::PrepareVft, Self::Env>;
    }

    pub struct VftStressImpl;

    impl sails_rs::client::Identifiable for VftStressImpl {
        const INTERFACE_ID: sails_rs::InterfaceId =
            sails_rs::InterfaceId::from_bytes_8([1, 131, 167, 200, 89, 93, 8, 160]);
    }

    impl<E: sails_rs::client::GearEnv> VftStress for sails_rs::client::Service<VftStressImpl, E> {
        type Env = E;
        fn bench_echo_vft_args(
            &mut self,
            backend: VftStorageBackend,
            op: VftTransferOp,
            seed: u32,
        ) -> sails_rs::client::PendingCall<io::BenchEchoVftArgs, Self::Env> {
            self.pending_call((backend, op, seed))
        }
        fn bench_noop(&mut self) -> sails_rs::client::PendingCall<io::BenchNoop, Self::Env> {
            self.pending_call(())
        }
        fn bench_vft_approve_bool(
            &mut self,
            backend: VftStorageBackend,
            owner_seed: u32,
            spender_seed: u32,
        ) -> sails_rs::client::PendingCall<io::BenchVftApproveBool, Self::Env> {
            self.pending_call((backend, owner_seed, spender_seed))
        }
        fn bench_vft_approve_profile(
            &mut self,
            backend: VftStorageBackend,
            owner_seed: u32,
            spender_seed: u32,
        ) -> sails_rs::client::PendingCall<io::BenchVftApproveProfile, Self::Env> {
            self.pending_call((backend, owner_seed, spender_seed))
        }
        fn bench_vft_transfer(
            &mut self,
            backend: VftStorageBackend,
            op: VftTransferOp,
            seed: u32,
        ) -> sails_rs::client::PendingCall<io::BenchVftTransfer, Self::Env> {
            self.pending_call((backend, op, seed))
        }
        fn bench_vft_transfer_fresh_bool(
            &mut self,
            backend: VftStorageBackend,
            seed: u32,
        ) -> sails_rs::client::PendingCall<io::BenchVftTransferFreshBool, Self::Env> {
            self.pending_call((backend, seed))
        }
        fn bench_vft_transfer_fresh_profile(
            &mut self,
            backend: VftStorageBackend,
            seed: u32,
        ) -> sails_rs::client::PendingCall<io::BenchVftTransferFreshProfile, Self::Env> {
            self.pending_call((backend, seed))
        }
        fn bench_vft_transfer_profile(
            &mut self,
            backend: VftStorageBackend,
            op: VftTransferOp,
            seed: u32,
        ) -> sails_rs::client::PendingCall<io::BenchVftTransferProfile, Self::Env> {
            self.pending_call((backend, op, seed))
        }
        fn prepare_vft(
            &mut self,
            backend: VftStorageBackend,
            len: u32,
        ) -> sails_rs::client::PendingCall<io::PrepareVft, Self::Env> {
            self.pending_call((backend, len))
        }
    }

    pub mod io {
        use super::*;
        sails_rs::io_struct_impl!(BenchEchoVftArgs (backend: super::VftStorageBackend, op: super::VftTransferOp, seed: u32) -> bool, 0, <super::VftStressImpl as sails_rs::client::Identifiable>::INTERFACE_ID);
        sails_rs::io_struct_impl!(BenchNoop () -> bool, 1, <super::VftStressImpl as sails_rs::client::Identifiable>::INTERFACE_ID);
        sails_rs::io_struct_impl!(BenchVftApproveBool (backend: super::VftStorageBackend, owner_seed: u32, spender_seed: u32) -> bool, 2, <super::VftStressImpl as sails_rs::client::Identifiable>::INTERFACE_ID);
        sails_rs::io_struct_impl!(BenchVftApproveProfile (backend: super::VftStorageBackend, owner_seed: u32, spender_seed: u32) -> super::VftProfileResult, 3, <super::VftStressImpl as sails_rs::client::Identifiable>::INTERFACE_ID);
        sails_rs::io_struct_impl!(BenchVftTransfer (backend: super::VftStorageBackend, op: super::VftTransferOp, seed: u32) -> super::VftTransferResult, 4, <super::VftStressImpl as sails_rs::client::Identifiable>::INTERFACE_ID);
        sails_rs::io_struct_impl!(BenchVftTransferFreshBool (backend: super::VftStorageBackend, seed: u32) -> bool, 5, <super::VftStressImpl as sails_rs::client::Identifiable>::INTERFACE_ID);
        sails_rs::io_struct_impl!(BenchVftTransferFreshProfile (backend: super::VftStorageBackend, seed: u32) -> super::VftProfileResult, 6, <super::VftStressImpl as sails_rs::client::Identifiable>::INTERFACE_ID);
        sails_rs::io_struct_impl!(BenchVftTransferProfile (backend: super::VftStorageBackend, op: super::VftTransferOp, seed: u32) -> super::VftProfileResult, 7, <super::VftStressImpl as sails_rs::client::Identifiable>::INTERFACE_ID);
        sails_rs::io_struct_impl!(PrepareVft (backend: super::VftStorageBackend, len: u32) -> super::VftTransferResult, 8, <super::VftStressImpl as sails_rs::client::Identifiable>::INTERFACE_ID);
    }
}
