// Code generated by sails-client-gen-v2. DO NOT EDIT.
#[allow(unused_imports)]
use sails::{client::*, collections::*, prelude::*};
pub struct AllocStressProgram;

impl AllocStressProgram {
    pub const ROUTE_ID_ALLOC_STRESS: u8 = 1;
}

impl sails::client::Program for AllocStressProgram {}

pub trait AllocStress {
    type Env: sails::client::GearEnv;
    fn alloc_stress(&self) -> sails::client::Service<alloc_stress::AllocStressImpl, Self::Env>;
}

impl<E: sails::client::GearEnv> AllocStress for sails::client::Actor<AllocStressProgram, E> {
    type Env = E;
    fn alloc_stress(&self) -> sails::client::Service<alloc_stress::AllocStressImpl, Self::Env> {
        self.service(AllocStressProgram::ROUTE_ID_ALLOC_STRESS)
    }
}
pub trait AllocStressCtors {
    type Env: sails::client::GearEnv + sails::client::EnvWithCtor;
    fn new_for_bench(
        self,
    ) -> sails::client::PendingCtor<AllocStressProgram, io::NewForBench, Self::Env>;
}

impl<E: sails::client::GearEnv + sails::client::EnvWithCtor> AllocStressCtors
    for sails::client::Deployment<AllocStressProgram, E>
{
    type Env = E;
    fn new_for_bench(
        self,
    ) -> sails::client::PendingCtor<AllocStressProgram, io::NewForBench, Self::Env> {
        self.pending_ctor(())
    }
}

pub mod io {
    use super::*;
    sails::io_struct_impl!(NewForBench () -> (), 0);
}

pub mod alloc_stress {
    use super::*;

    #[sails::sails_type(crate = sails)]
    #[derive(PartialEq, Clone, Debug)]
    pub struct AllocStressResult {
        pub inner: Vec<u8>,
    }

    pub trait AllocStress {
        type Env: sails::client::GearEnv;
        fn alloc_stress(
            &mut self,
            n: u32,
        ) -> sails::client::PendingCall<io::AllocStress, Self::Env>;
    }

    pub struct AllocStressImpl;

    impl sails::client::Identifiable for AllocStressImpl {
        const INTERFACE_ID: sails::InterfaceId =
            sails::InterfaceId::from_bytes_8([9, 48, 193, 195, 84, 117, 173, 52]);
    }

    impl<E: sails::client::GearEnv> AllocStress for sails::client::Service<AllocStressImpl, E> {
        type Env = E;
        fn alloc_stress(
            &mut self,
            n: u32,
        ) -> sails::client::PendingCall<io::AllocStress, Self::Env> {
            self.pending_call((n,))
        }
    }

    pub mod io {
        use super::*;
        sails::io_struct_impl!(AllocStress (n: u32) -> super::AllocStressResult, 0, <super::AllocStressImpl as sails::client::Identifiable>::INTERFACE_ID);
    }
}
