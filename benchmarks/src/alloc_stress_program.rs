// Code generated by sails-client-gen. DO NOT EDIT.
#[allow(unused_imports)]
use sails_rs::collections::BTreeMap;
#[allow(unused_imports)]
use sails_rs::{
    String,
    calls::{Activation, Call, Query, Remoting, RemotingAction},
    prelude::*,
};
pub struct AllocStressProgramFactory<R> {
    #[allow(dead_code)]
    remoting: R,
}

impl<R> AllocStressProgramFactory<R> {
    #[allow(unused)]
    pub fn new(remoting: R) -> Self {
        Self { remoting }
    }
}

impl<R: Remoting + Clone> traits::AllocStressProgramFactory for AllocStressProgramFactory<R> {
    type Args = R::Args;
    fn new_for_bench(&self) -> impl Activation<Args = R::Args> {
        RemotingAction::<_, alloc_stress_program_factory::io::NewForBench>::new(
            self.remoting.clone(),
            (),
        )
    }
}

pub mod alloc_stress_program_factory {
    use super::*;
    pub mod io {
        use super::*;
        use sails_rs::calls::ActionIo;
        pub struct NewForBench(());

        impl NewForBench {
            #[allow(dead_code)]
            pub fn encode_call() -> Vec<u8> {
                <NewForBench as ActionIo>::encode_call(&())
            }
        }

        impl ActionIo for NewForBench {
            const ROUTE: &'static [u8] = &[44, 78, 101, 119, 70, 111, 114, 66, 101, 110, 99, 104];
            type Params = ();
            type Reply = ();
        }
    }
}
pub struct AllocStress<R> {
    remoting: R,
}

impl<R> AllocStress<R> {
    pub fn new(remoting: R) -> Self {
        Self { remoting }
    }
}

impl<R: Remoting + Clone> traits::AllocStress for AllocStress<R> {
    type Args = R::Args;
    fn alloc_stress(&mut self, n: u32) -> impl Call<Output = AllocStressResult, Args = R::Args> {
        RemotingAction::<_, alloc_stress::io::AllocStress>::new(self.remoting.clone(), n)
    }
}

pub mod alloc_stress {
    use super::*;

    pub mod io {
        use super::*;
        use sails_rs::calls::ActionIo;
        pub struct AllocStress(());

        impl AllocStress {
            #[allow(dead_code)]
            pub fn encode_call(n: u32) -> Vec<u8> {
                <AllocStress as ActionIo>::encode_call(&n)
            }
        }

        impl ActionIo for AllocStress {
            const ROUTE: &'static [u8] = &[
                44, 65, 108, 108, 111, 99, 83, 116, 114, 101, 115, 115, 44, 65, 108, 108, 111, 99,
                83, 116, 114, 101, 115, 115,
            ];
            type Params = u32;
            type Reply = super::AllocStressResult;
        }
    }
}
#[derive(PartialEq, Clone, Debug, Encode, Decode, TypeInfo)]
#[codec(crate = sails_rs::scale_codec)]
#[scale_info(crate = sails_rs::scale_info)]
pub struct AllocStressResult {
    pub inner: Vec<u8>,
}

pub mod traits {
    use super::*;
    #[allow(dead_code)]
    pub trait AllocStressProgramFactory {
        type Args;
        fn new_for_bench(&self) -> impl Activation<Args = Self::Args>;
    }

    #[allow(clippy::type_complexity)]
    pub trait AllocStress {
        type Args;
        fn alloc_stress(
            &mut self,
            n: u32,
        ) -> impl Call<Output = AllocStressResult, Args = Self::Args>;
    }
}
