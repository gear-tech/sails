// Code generated by sails-client-gen-v2. DO NOT EDIT.
#[allow(unused_imports)]
use sails::{client::*, collections::*, prelude::*};
pub struct CounterBenchProgram;

impl CounterBenchProgram {
    pub const ROUTE_ID_COUNTER_BENCH: u8 = 1;
}

impl sails::client::Program for CounterBenchProgram {}

pub trait CounterBench {
    type Env: sails::client::GearEnv;
    fn counter_bench(&self) -> sails::client::Service<counter_bench::CounterBenchImpl, Self::Env>;
}

impl<E: sails::client::GearEnv> CounterBench for sails::client::Actor<CounterBenchProgram, E> {
    type Env = E;
    fn counter_bench(&self) -> sails::client::Service<counter_bench::CounterBenchImpl, Self::Env> {
        self.service(CounterBenchProgram::ROUTE_ID_COUNTER_BENCH)
    }
}
pub trait CounterBenchCtors {
    type Env: sails::client::GearEnv + sails::client::EnvWithCtor;
    fn new_for_bench(
        self,
    ) -> sails::client::PendingCtor<CounterBenchProgram, io::NewForBench, Self::Env>;
}

impl<E: sails::client::GearEnv + sails::client::EnvWithCtor> CounterBenchCtors
    for sails::client::Deployment<CounterBenchProgram, E>
{
    type Env = E;
    fn new_for_bench(
        self,
    ) -> sails::client::PendingCtor<CounterBenchProgram, io::NewForBench, Self::Env> {
        self.pending_ctor(())
    }
}

pub mod io {
    use super::*;
    sails::io_struct_impl!(NewForBench () -> (), 0);
}

pub mod counter_bench {
    use super::*;

    pub trait CounterBench {
        type Env: sails::client::GearEnv;
        fn inc(&mut self) -> sails::client::PendingCall<io::Inc, Self::Env>;
        fn inc_async(&mut self) -> sails::client::PendingCall<io::IncAsync, Self::Env>;
    }

    pub struct CounterBenchImpl;

    impl sails::client::Identifiable for CounterBenchImpl {
        const INTERFACE_ID: sails::InterfaceId =
            sails::InterfaceId::from_bytes_8([149, 170, 24, 82, 218, 19, 238, 13]);
    }

    impl<E: sails::client::GearEnv> CounterBench for sails::client::Service<CounterBenchImpl, E> {
        type Env = E;
        fn inc(&mut self) -> sails::client::PendingCall<io::Inc, Self::Env> {
            self.pending_call(())
        }
        fn inc_async(&mut self) -> sails::client::PendingCall<io::IncAsync, Self::Env> {
            self.pending_call(())
        }
    }

    pub mod io {
        use super::*;
        sails::io_struct_impl!(Inc () -> u64, 0, <super::CounterBenchImpl as sails::client::Identifiable>::INTERFACE_ID);
        sails::io_struct_impl!(IncAsync () -> u64, 1, <super::CounterBenchImpl as sails::client::Identifiable>::INTERFACE_ID);
    }
}
