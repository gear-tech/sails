// Code generated by sails-client-gen-v2. DO NOT EDIT.
#[allow(unused_imports)]
use sails_rs::{client::*, collections::*, prelude::*};
pub struct StorageMillionProgram;

impl StorageMillionProgram {
    pub const ROUTE_ID_STORAGE_MILLION: u8 = 1;
}

impl sails_rs::client::Program for StorageMillionProgram {}

pub trait StorageMillion {
    type Env: sails_rs::client::GearEnv;
    fn storage_million(
        &self,
    ) -> sails_rs::client::Service<storage_million::StorageMillionImpl, Self::Env>;
}

impl<E: sails_rs::client::GearEnv> StorageMillion
    for sails_rs::client::Actor<StorageMillionProgram, E>
{
    type Env = E;
    fn storage_million(
        &self,
    ) -> sails_rs::client::Service<storage_million::StorageMillionImpl, Self::Env> {
        self.service(StorageMillionProgram::ROUTE_ID_STORAGE_MILLION)
    }
}
pub trait StorageMillionCtors {
    type Env: sails_rs::client::GearEnv;
    fn new_for_bench(
        self,
    ) -> sails_rs::client::PendingCtor<StorageMillionProgram, io::NewForBench, Self::Env>;
}

impl<E: sails_rs::client::GearEnv> StorageMillionCtors
    for sails_rs::client::Deployment<StorageMillionProgram, E>
{
    type Env = E;
    fn new_for_bench(
        self,
    ) -> sails_rs::client::PendingCtor<StorageMillionProgram, io::NewForBench, Self::Env> {
        self.pending_ctor(())
    }
}

pub mod io {
    use super::*;
    sails_rs::io_struct_impl!(NewForBench () -> (), 0);
}

pub mod storage_million {
    use super::*;

    #[sails_rs::sails_type(crate = sails_rs)]
    #[derive(PartialEq, Clone, Debug)]
    pub enum MillionStorageBackend {
        GenericStatic,
        WatActorStatic,
        MixedActorStatic,
        TagActorStatic,
        TagU64ActorStatic,
        ControlActorStatic,
        PageLocalActorStatic,
        GroupedActorPages2,
        GroupedActorPages4,
        GroupedActorPages8,
        GroupedActorPages16,
        GroupedActorPages32,
        GroupedActorPages64,
        GroupedActorPages128,
    }
    #[sails_rs::sails_type(crate = sails_rs)]
    #[derive(PartialEq, Clone, Debug)]
    pub struct MillionStorageBenchResult {
        pub value: U256,
        pub len: u32,
        pub existed: bool,
    }
    #[sails_rs::sails_type(crate = sails_rs)]
    #[derive(PartialEq, Clone, Debug)]
    pub enum MillionStorageOp {
        InsertFresh,
        UpdateExisting,
        ReadExisting,
        ReadMissing,
        Remove,
    }
    #[sails_rs::sails_type(crate = sails_rs)]
    #[derive(PartialEq, Clone, Debug)]
    pub enum MillionVftBackend {
        BTree,
        HashMap,
        GenericStatic,
        GenericStaticFused,
        GenericStaticFast,
        WatActorStatic,
        MixedActorStatic,
        MixedActorFast,
        TagActorStatic,
        TagU64ActorStatic,
        ControlActorStatic,
        PageLocalActorStatic,
        GroupedActorPages64,
        GroupedActorPages128,
        InlineOwnerAccountU256,
    }
    #[sails_rs::sails_type(crate = sails_rs)]
    #[derive(PartialEq, Clone, Debug)]
    pub enum MillionVftTransferOp {
        Transfer,
        TransferFrom,
    }
    #[sails_rs::sails_type(crate = sails_rs)]
    #[derive(PartialEq, Clone, Debug)]
    pub struct MillionVftTransferResult {
        pub from_balance: U256,
        pub to_balance: U256,
        pub allowance: U256,
        pub balance_len: u32,
        pub allowance_len: u32,
        pub transferred: bool,
    }

    pub trait StorageMillion {
        type Env: sails_rs::client::GearEnv;
        fn bench(
            &mut self,
            backend: MillionStorageBackend,
            op: MillionStorageOp,
            seed: u32,
        ) -> sails_rs::client::PendingCall<io::Bench, Self::Env>;
        fn bench_many(
            &mut self,
            backend: MillionStorageBackend,
            op: MillionStorageOp,
            start_seed: u32,
            count: u32,
        ) -> sails_rs::client::PendingCall<io::BenchMany, Self::Env>;
        fn bench_vft_approve_bool(
            &mut self,
            backend: MillionVftBackend,
            owner_seed: u32,
            spender_seed: u32,
        ) -> sails_rs::client::PendingCall<io::BenchVftApproveBool, Self::Env>;
        fn bench_vft_transfer(
            &mut self,
            backend: MillionVftBackend,
            op: MillionVftTransferOp,
            seed: u32,
        ) -> sails_rs::client::PendingCall<io::BenchVftTransfer, Self::Env>;
        fn bench_vft_transfer_bool(
            &mut self,
            backend: MillionVftBackend,
            op: MillionVftTransferOp,
            seed: u32,
        ) -> sails_rs::client::PendingCall<io::BenchVftTransferBool, Self::Env>;
        fn bench_vft_transfer_fresh_bool(
            &mut self,
            backend: MillionVftBackend,
            seed: u32,
        ) -> sails_rs::client::PendingCall<io::BenchVftTransferFreshBool, Self::Env>;
        fn bench_vft_transfer_from_spender_bool(
            &mut self,
            backend: MillionVftBackend,
            owner_seed: u32,
            spender_seed: u32,
        ) -> sails_rs::client::PendingCall<io::BenchVftTransferFromSpenderBool, Self::Env>;
        fn prepare_chunk(
            &mut self,
            backend: MillionStorageBackend,
            start: u32,
            len: u32,
        ) -> sails_rs::client::PendingCall<io::PrepareChunk, Self::Env>;
        fn prepare_vft_chunk(
            &mut self,
            backend: MillionVftBackend,
            start: u32,
            len: u32,
        ) -> sails_rs::client::PendingCall<io::PrepareVftChunk, Self::Env>;
    }

    pub struct StorageMillionImpl;

    impl sails_rs::client::Identifiable for StorageMillionImpl {
        const INTERFACE_ID: sails_rs::InterfaceId =
            sails_rs::InterfaceId::from_bytes_8([69, 52, 241, 61, 146, 22, 252, 107]);
    }

    impl<E: sails_rs::client::GearEnv> StorageMillion
        for sails_rs::client::Service<StorageMillionImpl, E>
    {
        type Env = E;
        fn bench(
            &mut self,
            backend: MillionStorageBackend,
            op: MillionStorageOp,
            seed: u32,
        ) -> sails_rs::client::PendingCall<io::Bench, Self::Env> {
            self.pending_call((backend, op, seed))
        }
        fn bench_many(
            &mut self,
            backend: MillionStorageBackend,
            op: MillionStorageOp,
            start_seed: u32,
            count: u32,
        ) -> sails_rs::client::PendingCall<io::BenchMany, Self::Env> {
            self.pending_call((backend, op, start_seed, count))
        }
        fn bench_vft_approve_bool(
            &mut self,
            backend: MillionVftBackend,
            owner_seed: u32,
            spender_seed: u32,
        ) -> sails_rs::client::PendingCall<io::BenchVftApproveBool, Self::Env> {
            self.pending_call((backend, owner_seed, spender_seed))
        }
        fn bench_vft_transfer(
            &mut self,
            backend: MillionVftBackend,
            op: MillionVftTransferOp,
            seed: u32,
        ) -> sails_rs::client::PendingCall<io::BenchVftTransfer, Self::Env> {
            self.pending_call((backend, op, seed))
        }
        fn bench_vft_transfer_bool(
            &mut self,
            backend: MillionVftBackend,
            op: MillionVftTransferOp,
            seed: u32,
        ) -> sails_rs::client::PendingCall<io::BenchVftTransferBool, Self::Env> {
            self.pending_call((backend, op, seed))
        }
        fn bench_vft_transfer_fresh_bool(
            &mut self,
            backend: MillionVftBackend,
            seed: u32,
        ) -> sails_rs::client::PendingCall<io::BenchVftTransferFreshBool, Self::Env> {
            self.pending_call((backend, seed))
        }
        fn bench_vft_transfer_from_spender_bool(
            &mut self,
            backend: MillionVftBackend,
            owner_seed: u32,
            spender_seed: u32,
        ) -> sails_rs::client::PendingCall<io::BenchVftTransferFromSpenderBool, Self::Env> {
            self.pending_call((backend, owner_seed, spender_seed))
        }
        fn prepare_chunk(
            &mut self,
            backend: MillionStorageBackend,
            start: u32,
            len: u32,
        ) -> sails_rs::client::PendingCall<io::PrepareChunk, Self::Env> {
            self.pending_call((backend, start, len))
        }
        fn prepare_vft_chunk(
            &mut self,
            backend: MillionVftBackend,
            start: u32,
            len: u32,
        ) -> sails_rs::client::PendingCall<io::PrepareVftChunk, Self::Env> {
            self.pending_call((backend, start, len))
        }
    }

    pub mod io {
        use super::*;
        sails_rs::io_struct_impl!(Bench (backend: super::MillionStorageBackend, op: super::MillionStorageOp, seed: u32) -> super::MillionStorageBenchResult, 0, <super::StorageMillionImpl as sails_rs::client::Identifiable>::INTERFACE_ID);
        sails_rs::io_struct_impl!(BenchMany (backend: super::MillionStorageBackend, op: super::MillionStorageOp, start_seed: u32, count: u32) -> super::MillionStorageBenchResult, 1, <super::StorageMillionImpl as sails_rs::client::Identifiable>::INTERFACE_ID);
        sails_rs::io_struct_impl!(BenchVftApproveBool (backend: super::MillionVftBackend, owner_seed: u32, spender_seed: u32) -> bool, 2, <super::StorageMillionImpl as sails_rs::client::Identifiable>::INTERFACE_ID);
        sails_rs::io_struct_impl!(BenchVftTransfer (backend: super::MillionVftBackend, op: super::MillionVftTransferOp, seed: u32) -> super::MillionVftTransferResult, 3, <super::StorageMillionImpl as sails_rs::client::Identifiable>::INTERFACE_ID);
        sails_rs::io_struct_impl!(BenchVftTransferBool (backend: super::MillionVftBackend, op: super::MillionVftTransferOp, seed: u32) -> bool, 4, <super::StorageMillionImpl as sails_rs::client::Identifiable>::INTERFACE_ID);
        sails_rs::io_struct_impl!(BenchVftTransferFreshBool (backend: super::MillionVftBackend, seed: u32) -> bool, 5, <super::StorageMillionImpl as sails_rs::client::Identifiable>::INTERFACE_ID);
        sails_rs::io_struct_impl!(BenchVftTransferFromSpenderBool (backend: super::MillionVftBackend, owner_seed: u32, spender_seed: u32) -> bool, 6, <super::StorageMillionImpl as sails_rs::client::Identifiable>::INTERFACE_ID);
        sails_rs::io_struct_impl!(PrepareChunk (backend: super::MillionStorageBackend, start: u32, len: u32) -> super::MillionStorageBenchResult, 7, <super::StorageMillionImpl as sails_rs::client::Identifiable>::INTERFACE_ID);
        sails_rs::io_struct_impl!(PrepareVftChunk (backend: super::MillionVftBackend, start: u32, len: u32) -> super::MillionVftTransferResult, 8, <super::StorageMillionImpl as sails_rs::client::Identifiable>::INTERFACE_ID);
    }
}
