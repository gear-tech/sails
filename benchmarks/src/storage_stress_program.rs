// Code generated by sails-client-gen-v2. DO NOT EDIT.
#[allow(unused_imports)]
use sails_rs::{client::*, collections::*, prelude::*};
pub struct StorageStressProgram;

impl StorageStressProgram {
    pub const ROUTE_ID_STORAGE_STRESS: u8 = 1;
}

impl sails_rs::client::Program for StorageStressProgram {}

pub trait StorageStress {
    type Env: sails_rs::client::GearEnv;
    fn storage_stress(
        &self,
    ) -> sails_rs::client::Service<storage_stress::StorageStressImpl, Self::Env>;
}

impl<E: sails_rs::client::GearEnv> StorageStress
    for sails_rs::client::Actor<StorageStressProgram, E>
{
    type Env = E;
    fn storage_stress(
        &self,
    ) -> sails_rs::client::Service<storage_stress::StorageStressImpl, Self::Env> {
        self.service(StorageStressProgram::ROUTE_ID_STORAGE_STRESS)
    }
}
pub trait StorageStressCtors {
    type Env: sails_rs::client::GearEnv;
    fn new_for_bench(
        self,
    ) -> sails_rs::client::PendingCtor<StorageStressProgram, io::NewForBench, Self::Env>;
}

impl<E: sails_rs::client::GearEnv> StorageStressCtors
    for sails_rs::client::Deployment<StorageStressProgram, E>
{
    type Env = E;
    fn new_for_bench(
        self,
    ) -> sails_rs::client::PendingCtor<StorageStressProgram, io::NewForBench, Self::Env> {
        self.pending_ctor(())
    }
}

pub mod io {
    use super::*;
    sails_rs::io_struct_impl!(NewForBench () -> (), 0);
}

pub mod storage_stress {
    use super::*;

    #[sails_rs::sails_type(crate = sails_rs)]
    #[derive(PartialEq, Clone, Debug)]
    pub enum StorageBackend {
        HashMap,
        Fixed,
        RawStatic,
        SailsFixed,
        SailsStatic,
    }
    #[sails_rs::sails_type(crate = sails_rs)]
    #[derive(PartialEq, Clone, Debug)]
    pub struct StorageBenchResult {
        pub value: U256,
        pub len: u32,
        pub existed: bool,
    }
    #[sails_rs::sails_type(crate = sails_rs)]
    #[derive(PartialEq, Clone, Debug)]
    pub enum StorageMap {
        Balance,
        Allowance,
    }
    #[sails_rs::sails_type(crate = sails_rs)]
    #[derive(PartialEq, Clone, Debug)]
    pub enum StorageOp {
        InsertFresh,
        UpdateExisting,
        ReadExisting,
        ReadMissing,
        Remove,
    }

    pub trait StorageStress {
        type Env: sails_rs::client::GearEnv;
        fn bench(
            &mut self,
            backend: StorageBackend,
            map: StorageMap,
            op: StorageOp,
            seed: u32,
        ) -> sails_rs::client::PendingCall<io::Bench, Self::Env>;
        fn prepare(
            &mut self,
            backend: StorageBackend,
            map: StorageMap,
            len: u32,
        ) -> sails_rs::client::PendingCall<io::Prepare, Self::Env>;
    }

    pub struct StorageStressImpl;

    impl sails_rs::client::Identifiable for StorageStressImpl {
        const INTERFACE_ID: sails_rs::InterfaceId =
            sails_rs::InterfaceId::from_bytes_8([102, 51, 57, 17, 133, 13, 155, 16]);
    }

    impl<E: sails_rs::client::GearEnv> StorageStress
        for sails_rs::client::Service<StorageStressImpl, E>
    {
        type Env = E;
        fn bench(
            &mut self,
            backend: StorageBackend,
            map: StorageMap,
            op: StorageOp,
            seed: u32,
        ) -> sails_rs::client::PendingCall<io::Bench, Self::Env> {
            self.pending_call((backend, map, op, seed))
        }
        fn prepare(
            &mut self,
            backend: StorageBackend,
            map: StorageMap,
            len: u32,
        ) -> sails_rs::client::PendingCall<io::Prepare, Self::Env> {
            self.pending_call((backend, map, len))
        }
    }

    pub mod io {
        use super::*;
        sails_rs::io_struct_impl!(Bench (backend: super::StorageBackend, map: super::StorageMap, op: super::StorageOp, seed: u32) -> super::StorageBenchResult, 0, <super::StorageStressImpl as sails_rs::client::Identifiable>::INTERFACE_ID);
        sails_rs::io_struct_impl!(Prepare (backend: super::StorageBackend, map: super::StorageMap, len: u32) -> super::StorageBenchResult, 1, <super::StorageStressImpl as sails_rs::client::Identifiable>::INTERFACE_ID);
    }
}
