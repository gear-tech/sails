// Code generated by sails-client-gen-v2. DO NOT EDIT.
#[allow(unused_imports)]
use sails::{client::*, collections::*, prelude::*};
pub struct PingPongProgram;

impl PingPongProgram {
    pub const ROUTE_ID_PING_PONG_SERVICE: u8 = 1;
}

impl sails::client::Program for PingPongProgram {}

pub trait PingPong {
    type Env: sails::client::GearEnv;
    fn ping_pong_service(
        &self,
    ) -> sails::client::Service<ping_pong_service::PingPongServiceImpl, Self::Env>;
}

impl<E: sails::client::GearEnv> PingPong for sails::client::Actor<PingPongProgram, E> {
    type Env = E;
    fn ping_pong_service(
        &self,
    ) -> sails::client::Service<ping_pong_service::PingPongServiceImpl, Self::Env> {
        self.service(PingPongProgram::ROUTE_ID_PING_PONG_SERVICE)
    }
}
pub trait PingPongCtors {
    type Env: sails::client::GearEnv + sails::client::EnvWithCtor;
    fn new_for_bench(
        self,
    ) -> sails::client::PendingCtor<PingPongProgram, io::NewForBench, Self::Env>;
}

impl<E: sails::client::GearEnv + sails::client::EnvWithCtor> PingPongCtors
    for sails::client::Deployment<PingPongProgram, E>
{
    type Env = E;
    fn new_for_bench(
        self,
    ) -> sails::client::PendingCtor<PingPongProgram, io::NewForBench, Self::Env> {
        self.pending_ctor(())
    }
}

pub mod io {
    use super::*;
    sails::io_struct_impl!(NewForBench () -> (), 0);
}

pub mod ping_pong_service {
    use super::*;

    #[sails::sails_type(crate = sails)]
    #[derive(PartialEq, Clone, Debug)]
    pub enum PingPongPayload {
        Start(ActorId),
        Ping,
        Pong,
        Finished,
    }

    pub trait PingPongService {
        type Env: sails::client::GearEnv;
        fn ping(
            &mut self,
            payload: PingPongPayload,
        ) -> sails::client::PendingCall<io::Ping, Self::Env>;
    }

    pub struct PingPongServiceImpl;

    impl sails::client::Identifiable for PingPongServiceImpl {
        const INTERFACE_ID: sails::InterfaceId =
            sails::InterfaceId::from_bytes_8([106, 114, 150, 138, 76, 98, 231, 215]);
    }

    impl<E: sails::client::GearEnv> PingPongService for sails::client::Service<PingPongServiceImpl, E> {
        type Env = E;
        fn ping(
            &mut self,
            payload: PingPongPayload,
        ) -> sails::client::PendingCall<io::Ping, Self::Env> {
            self.pending_call((payload,))
        }
    }

    pub mod io {
        use super::*;
        sails::io_struct_impl!(Ping (payload: super::PingPongPayload) -> super::PingPongPayload, 0, <super::PingPongServiceImpl as sails::client::Identifiable>::INTERFACE_ID);
    }
}
