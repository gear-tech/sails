[package]
name = "ethapp-with-events"
version.workspace = true
edition.workspace = true

[dependencies]
sails = { workspace = true, features = ["ethexe"] }

[build-dependencies]
sails = { workspace = true, features = ["ethexe", "wasm-builder"] }

[dev-dependencies]
insta.workspace = true
sails = { workspace = true, features = ["ethexe", "gtest", "idl-gen"] }
tokio = { workspace = true, features = ["rt", "macros"] }
